import styles from './Banner.module.scss'

const Banner = () => {
  return <section class={styles.Banner}>
    <header class={styles.Header}>
      <h1>Ewan Breakey</h1>
    </header>
  </section>
}

export default Banner
